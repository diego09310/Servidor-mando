#!/bin/bash

# Ruta al programa que realiza el reconocimiento
LID_RPi_DIR=/home/diego/Dropbox/Universidad/SDG2_Octave_sin_normalizar/lidonrpi_remote/LID_RPi/audio

NAME=`basename $1`

#Renombra tmpAudio según el idioma reconocido y lo mueve al directorio correspondiente
mv ${LID_RPi_DIR}/${NAME}.wav ${LID_RPi_DIR}/${2}/${2}${NAME}.wav


